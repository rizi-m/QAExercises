public class BlackJack {
	
	private static boolean getWinner(int firstNumber, int secondNumber) {
		return ((firstNumber - 21 >= 0 && 
				secondDifference >= 0 && 
				firstDifference < secondDifference) 
				||
				(firstDifference >= 0 && 
				secondDifference < 0))
	}
	
	public static int play(int firstNumber, int secondNumber) throws IllegalArgumentException {
		if (firstNumber < 1 || secondNumber < 1) {
			throw new IllegalArgumentException("Values must be greater than 0.");
		}


	}
	
}
